#!/usr/bin/env bash
# 仓内稳定入口。CI 只调用它，命令细节封在这里，
# 避免 .agent-doctor.yaml 和 workflow 各写一份命令产生漂移。
set -euo pipefail
cd "$(dirname "$0")/.."
go build ./...
go vet ./...
go test ./...
python3 tools/agent-doctor-bootstrap_test.py
